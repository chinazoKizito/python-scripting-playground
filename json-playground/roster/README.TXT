This project features a python script roster.py which creates an sqlite database "rosterdb.sqlite" and a schema for the contents of  the json file 'roster_data_sammple.json.'
The scripts also cleans up this data and inserts it into the database for ease of access and analysis.

projects credit ==> Dr. Chuck of "python for everybody"